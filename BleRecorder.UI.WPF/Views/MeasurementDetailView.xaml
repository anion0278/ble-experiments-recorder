<UserControl x:Class="BleRecorder.UI.WPF.Views.MeasurementDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:toolkit="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:testSubject="clr-namespace:BleRecorder.Models.TestSubject;assembly=BleRecorder.Models"
             xmlns:livecharts="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:viewModels="clr-namespace:BleRecorder.UI.WPF.ViewModels"
             xmlns:extensions="clr-namespace:BleRecorder.UI.WPF.Extensions"
             xmlns:System="clr-namespace:System;assembly=System.Runtime"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="600" 
             d:DataContext="{d:DesignInstance viewModels:MeasurementDetailViewModel, IsDesignTimeCreatable=True}">
    <UserControl.Resources>
        <Style x:Key="MeasurementParamComboBoxStyle" TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
            <Setter Property="ToolTipService.ShowOnDisabled" Value="True"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Width" Value="160"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="IsEnabled" Value="{Binding ForceValues,Mode=OneWay, Converter={StaticResource collectionLengthToBooleanConverter}}"/>
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding Path=., Converter={StaticResource enumDescriptionConverter}}" />
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="ToolTip" Value="Measurement parameters can only be changed if the measurement data is empty."></Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <ObjectDataProvider x:Key="MeasurementTypeEnum" MethodName="GetValues" ObjectType="{x:Type System:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="testSubject:MeasurementType"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>

        <ObjectDataProvider x:Key="MeasurementSiteEnum" MethodName="GetValues" ObjectType="{x:Type System:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="testSubject:MeasurementSite"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>

        <ObjectDataProvider x:Key="PositionDuringMeasurementEnum" MethodName="GetValues" ObjectType="{x:Type System:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="testSubject:PositionDuringMeasurement"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>

    <ScrollViewer Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Content="Short description" />
            <TextBox Grid.Row="1" MaxLength="40" 
                     Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" />

            <Label Grid.Row="2" Content="Notes"/>
            <TextBox Grid.Row="3" Style="{StaticResource NotesTextBox}"
                 Text="{Binding Notes}" />

            <Grid Grid.Row="4">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="Measurement date:" />
                <Label Grid.Row="0" Grid.Column="1"  FontWeight="Bold" VerticalAlignment="Center" 
                           Content="{Binding Date, StringFormat='{}{0:dd MMM. yyyy, HH:mm}', TargetNullValue='Not measured yet'}" />

                <Label Grid.Row="1" Grid.Column="0"  Content="Test subject:"/>
                <Label Grid.Row="1" Grid.Column="1" FontWeight="Bold" 
                       Content="{Binding Measurement.TestSubject.FullName}"/>

                <Label Grid.Row="2" Grid.Column="0"  Content="Measurement type:" />
                <ComboBox Grid.Row="2" Grid.Column="1" SelectedItem="{Binding Type}" Style="{StaticResource MeasurementParamComboBoxStyle}"
                          ItemsSource="{Binding Source={StaticResource MeasurementTypeEnum}}" />

                <Label Grid.Row="3" Grid.Column="0"  Content="Measurement site:" />
                <ComboBox Grid.Row="3" Grid.Column="1" SelectedItem="{Binding Site}" Style="{StaticResource MeasurementParamComboBoxStyle}"
                          ItemsSource="{Binding Source={StaticResource MeasurementSiteEnum}}" />

                <Label Grid.Row="4" Grid.Column="0"  Content="Test subject position:" />
                <ComboBox Grid.Row="4" Grid.Column="1" SelectedItem="{Binding Position}" Style="{StaticResource MeasurementParamComboBoxStyle}"
                          ItemsSource="{Binding Source={StaticResource PositionDuringMeasurementEnum}}" />

            </Grid>

            <livecharts:CartesianChart Grid.Row="5"  DisableAnimations="True" Margin="4" Height="230" >
                <livecharts:CartesianChart.AxisY>
                    <livecharts:Axis MaxValue="120" MinValue="0" Title="Contraction Force [N]" />
                </livecharts:CartesianChart.AxisY>

                <livecharts:CartesianChart.AxisX>
                    <livecharts:Axis Title="Time [s]" />
                </livecharts:CartesianChart.AxisX>

                <livecharts:CartesianChart.SeriesColors>
                    <livecharts:ColorsCollection>
                        <Color>Aqua</Color>
                        <Color R="50" G="40" B="130" />
                    </livecharts:ColorsCollection>
                </livecharts:CartesianChart.SeriesColors >

                <livecharts:CartesianChart.Series>
                    <livecharts:LineSeries Values="{Binding ForceValues}"></livecharts:LineSeries>
                </livecharts:CartesianChart.Series>
            </livecharts:CartesianChart>

            <Grid Grid.Row="6" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="Start measurement" Command="{Binding StartMeasurementCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">

                            <Style.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="ToolTip" Value="Check device connection" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Grid.Column="1" Content="Stop measurement" Command="{Binding StopMeasurementCommand}"/>
                <Button Grid.Column="2" Content="Clean data" Command="{Binding CleanRecordedDataCommand}"/>
            </Grid>

            <StackPanel Grid.Row="7" Orientation="Horizontal">
                <Button Content="Save" extensions:RelayCommandExtensions.IsCommandUpdateEnabled="True"
                    Command="{Binding SaveCommand}"/>
                <Button Content="Delete" Command="{Binding DeleteCommand}"/>
            </StackPanel>

        </Grid>
    </ScrollViewer>
</UserControl>
